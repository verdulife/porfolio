<section id="presentation" class="flex pt-16 md:pt-10">
  <p class="slide-text text-2xl leading-tight md:text-6xl">
    <span>
      I'm a graphic designer and frontend developer from Barcelona. With more
      than 15 years of experience in the industry, I think design first its core
      of all ui/ux development.
    </span>
  </p>
</section>

<style>
  .slide-text span {
    color: rgba(255, 255, 255, 0.1);
    background: linear-gradient(gray, gray) no-repeat;
    background-clip: text;
  }
</style>

<script>
  import { scroll } from "motion";
  import { animate } from "motion/mini";

  const target = document.querySelector("#presentation") as HTMLElement;
  const animation = animate(
    ".slide-text span",
    { backgroundSize: ["100% 100%", "0% 100%"] },
    { ease: "linear" }
  );

  scroll(animation, { target });
</script>
